/** @type {import('next').NextConfig} */
const nextConfig = {
  // 必要な他の設定があればここに記述
  reactStrictMode: true, // ReactのStrictモードを有効にする例
  swcMinify: true, // SWCを使った高速なMinify
};

export default nextConfig;
